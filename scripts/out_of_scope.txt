# OUT OF SCOPE — excluded from the faithfulness denominator (NOT gaps, NOT intentional-divergences).
# These are milestones/features the owner has deferred. Distinct from intentional_divergences.txt
# (where ng is deliberately better and legacy is broken).
#
# @@fsm — PARKED (owner: not now). The whole regex-recognizer DSL milestone (M9).
fsm/
# R5 per-item @@[target(...)] — xfail (owner: not supported now; undocumented per-member filtering).
primary/89_attribute_per_item.fpy
primary/90_attribute_domain_field.fpy
