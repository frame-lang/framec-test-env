@@target c


@@system Stress_st_p1_many_dispatches__smoke {
    interface:
        bump()
        get_f(): int

    machine:
        $S0 {
            bump() {
                self->f = self->f + 1;
            }
            get_f(): int { @@:(self->f) }
        }

    domain:
        f: int = 0
}

#include <stdio.h>
#include <stdlib.h>
int main(void) {
    Stress_st_p1_many_dispatches__smoke* _inst = Stress_st_p1_many_dispatches__smoke_new();
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    Stress_st_p1_many_dispatches__smoke_bump(_inst);
    int _ret = (int)(intptr_t)Stress_st_p1_many_dispatches__smoke_get_f(_inst);
    if (_ret != 10) {
        printf("FAIL: expected ret=10, got %d\n", _ret);
        return 1;
    }
    printf("PASS: stress\n");
    return 0;
}
