@@[target("ruby")]


@@system Stress_st_p1_many_dispatches__smoke {
    interface:
        bump()
        get_f(): int

    machine:
        $S0 {
            bump() {
                self.f = self.f + 1
            }
            get_f(): int { @@:(self.f) }
        }

    domain:
        f: int = 0
}


def _fail(msg)
  puts("FAIL: #{msg}")
  exit(1)
end

_inst = Stress_st_p1_many_dispatches__smoke.new
_inst.bump
_inst.bump
_inst.bump
_inst.bump
_inst.bump
_inst.bump
_inst.bump
_inst.bump
_inst.bump
_inst.bump
_ret = _inst.get_f
_fail("expected ret=10, got #{_ret}") unless _ret == 10
puts("PASS: stress")
