@@[target("java")]


@@system Comments_cm_p4_native_machine_comments__t8 {
    interface:
        drive()
        get_f(): int

    machine:
        // top-level comment before $S0
        $S0 {
            // comment inside state, before handler
            drive() {
                this.f = 99;
            }
            // comment between handlers
            get_f(): int { @@:(this.f) }
        }
        // trailing comment

    domain:
        f: int = 0
}

class Driver {
    public static void main(String[] args) {
        var _inst = new Comments_cm_p4_native_machine_comments__t8();
        _inst.drive();
        int _ret = (int) _inst.get_f();
        if (_ret != 99) {
            System.out.println("FAIL: expected ret=99, got " + _ret);
            System.exit(1);
        }
        System.out.println("PASS: comments");
    }
}
