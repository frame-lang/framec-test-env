@@target go

package main

import "fmt"
import "os"


@@system StateArgs_sa_p8_arg_in_event__t4 {
    interface:
        Drive()
        Drive2(): int

    machine:
        $S0 {
            Drive() {
                -> $S1(100)
            }
        }
        $S1(x: int) {
            Drive2(): int {
                @@:return = x
            }
        }

    domain:
        f: int = 50
}


func _fail(msg string) { fmt.Println("FAIL: " + msg); os.Exit(1) }

func main() {
    sm := NewStateArgs_sa_p8_arg_in_event__t4()
    sm.Drive()
    ret := sm.Drive2()
    if ret != 100 { _fail(fmt.Sprintf("expected ret=100, got %d", ret)) }
    fmt.Println("PASS: state-args")
}