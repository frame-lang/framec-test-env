@@[target("typescript")]


@@system StateArgs_sa_p6_pingpong__t0 {
    interface:
        drive()
        get_x(): int

    machine:
        $S0 {
            drive() {
                -> $S1(1)
            }
        }
        $S1(x: int) {
            $>() {
                -> $S2(x * 2)
            }
        }
        $S2(y: int) {
            get_x(): int { @@:(y) }
        }

    domain:
        f: int = 0
}


function _fail(msg: string): never { console.log("FAIL: " + msg); process.exit(1); }

const _inst = new StateArgs_sa_p6_pingpong__t0();
_inst.drive();
const _ret: number = _inst.get_x();
if (_ret !== 2) { _fail("expected ret=2, got " + _ret); }
console.log("PASS: state-args");