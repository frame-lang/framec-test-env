@@target rust


@@system StateArgs_sa_p10_three_lvl_mixed__t1 {
    interface:
        drive()
        get_x(): int

    machine:
        $S0 {
            drive() {
                -> $Leaf(5)
            }
        }
        $Leaf => $Mid(x: int) {
            get_x(): int { @@:(x) }
        }
        $Mid => $Root {
        }
        $Root(x: int) {
        }

    domain:
        f: int = 0
}


fn _fail(msg: &str) -> ! { panic!("FAIL: {}", msg); }

fn main() {
    let mut _inst = StateArgs_sa_p10_three_lvl_mixed__t1::new();
    _inst.drive();
    let _ret = _inst.get_x();
    if _ret != 5 { _fail(&format!("expected ret=5, got {}", _ret)); }
    println!("PASS: state-args");
}