@@target go

package main

import "fmt"
import "os"


@@system StateArgs_sa_p5_multi_arg__t1 {
    interface:
        Drive()
        Get_x(): int

    machine:
        $S0 {
            Drive() {
                -> $S1(5, 5)
            }
        }
        $S1(x: int, y: int) {
            Get_x(): int { @@:(x + y) }
        }

    domain:
        f: int = 0
}


func _fail(msg string) { fmt.Println("FAIL: " + msg); os.Exit(1) }

func main() {
    sm := NewStateArgs_sa_p5_multi_arg__t1()
    sm.Drive()
    ret := sm.Get_x()
    if ret != 10 { _fail(fmt.Sprintf("expected ret=10, got %d", ret)) }
    fmt.Println("PASS: state-args")
}