@@[target("python_3")]
# @@expect-error: E111
#
# E111: a system declares each parameter once. Two `x` parameters
# in the system header should be rejected.

@@system DupParam(x: int, x: int) {
    interface:
        ping()
    machine:
        $A {
            ping() {}
        }
}
