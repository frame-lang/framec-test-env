@@[target("python_3")]
# @@expect-error: E815
#
# E815: `@@[create]` is a system-level attribute only. Attaching
# it to an operation (or any per-item position — interface method,
# handler, domain field) should be rejected.

@@system BadCreatePosition {
    operations:
        @@[create(make)]
        bogus_op(): int {}

    interface:
        ping(): int

    machine:
        $A {
            ping(): int { @@:(0) }
        }
}
