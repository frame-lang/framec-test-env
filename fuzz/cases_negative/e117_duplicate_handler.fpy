@@[target("python_3")]
# @@expect-error: E117
#
# E117: a state cannot declare two handlers for the same event.
# Two `ping()` handlers in `$A` should be rejected.

@@system DupHandler {
    interface:
        ping()
    machine:
        $A {
            ping() {}
            ping() {}
        }
}
