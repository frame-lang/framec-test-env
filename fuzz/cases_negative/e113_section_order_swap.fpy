@@[target("python_3")]
# @@expect-error: E113
#
# E113: machine and interface declared out of order. The required
# section order is operations → interface → machine → actions →
# domain. Putting machine before interface should be rejected.

@@system OutOfOrder {
    machine:
        $A {}
    interface:
        do_thing()
}
