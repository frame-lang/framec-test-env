@@[target("python_3")]
# @@expect-error: E116
#
# E116: state names within a machine must be unique. Two `$A`
# blocks should be rejected by the validator.

@@system DupState {
    interface:
        ping()
    machine:
        $A {
            ping() {}
        }
        $A {
            ping() {}
        }
}
