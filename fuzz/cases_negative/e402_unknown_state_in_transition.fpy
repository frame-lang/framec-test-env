@@[target("python_3")]
# @@expect-error: E402
#
# E402: a `-> $State` transition references a state that doesn't
# exist in the machine. Catches typos at compile time.

@@system UnknownState {
    interface:
        go()
    machine:
        $A {
            go() {
                -> $Nowhere
            }
        }
}
