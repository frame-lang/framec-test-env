@@target c


@@system MultiEvent_me_p1_three_event_same_state__t4 {
    interface:
        bump_a()
        bump_b()
        bump_c()
        get_f(): int

    machine:
        $S0 {
            bump_a() {
                self->f = self->f + 0;
            }
            bump_b() {
                self->f = self->f + 100;
            }
            bump_c() {
                self->f = self->f + 0;
            }
            get_f(): int { @@:(self->f) }
        }

    domain:
        f: int = 0
}

#include <stdio.h>
#include <stdlib.h>
int main(void) {
    MultiEvent_me_p1_three_event_same_state__t4* _inst = MultiEvent_me_p1_three_event_same_state__t4_new();
    MultiEvent_me_p1_three_event_same_state__t4_bump_a(_inst);
    MultiEvent_me_p1_three_event_same_state__t4_bump_b(_inst);
    MultiEvent_me_p1_three_event_same_state__t4_bump_c(_inst);
    int _ret = (int)(intptr_t)MultiEvent_me_p1_three_event_same_state__t4_get_f(_inst);
    if (_ret != 100) {
        printf("FAIL: expected ret=100, got %d\n", _ret);
        return 1;
    }
    printf("PASS: multievent\n");
    return 0;
}
