@@[target("c")]


@@system MultiEvent_me_p1_three_event_same_state__t2 {
    interface:
        bump_a()
        bump_b()
        bump_c()
        get_f(): int

    machine:
        $S0 {
            bump_a() {
                self->f = self->f + -3;
            }
            bump_b() {
                self->f = self->f + 4;
            }
            bump_c() {
                self->f = self->f + -3;
            }
            get_f(): int { @@:(self->f) }
        }

    domain:
        f: int = 0
}

#include <stdio.h>
#include <stdlib.h>
int main(void) {
    MultiEvent_me_p1_three_event_same_state__t2* _inst = MultiEvent_me_p1_three_event_same_state__t2_new();
    MultiEvent_me_p1_three_event_same_state__t2_bump_a(_inst);
    MultiEvent_me_p1_three_event_same_state__t2_bump_b(_inst);
    MultiEvent_me_p1_three_event_same_state__t2_bump_c(_inst);
    int _ret = (int)(intptr_t)MultiEvent_me_p1_three_event_same_state__t2_get_f(_inst);
    if (_ret != -2) {
        printf("FAIL: expected ret=-2, got %d\n", _ret);
        return 1;
    }
    printf("PASS: multievent\n");
    return 0;
}
