@@[target("typescript")]


@@system MultiEvent_me_p4_event_in_hsm_chain__t9 {
    interface:
        bump_a()
        drive()
        fwd_bump()
        get_f(): int

    machine:
        $S0 {
            drive() {
                -> $Child
            }
        }
        $Child => $Parent {
            bump_a() {
                this.f = this.f + -1;
            }
            fwd_bump() {
                => $^
            }
            get_f(): int { @@:(this.f) }
        }
        $Parent {
            fwd_bump() {
                this.f = this.f + 1;
            }
        }

    domain:
        f: int = 0
}


function _fail(msg: string): never { console.log("FAIL: " + msg); process.exit(1); }

const _inst = new MultiEvent_me_p4_event_in_hsm_chain__t9();
_inst.drive();
_inst.bump_a();
_inst.fwd_bump();
const _ret: number = _inst.get_f();
if (_ret !== 0) { _fail("expected ret=0, got " + _ret); }
console.log("PASS: multievent");
