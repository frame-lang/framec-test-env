@@[target("c")]


@@system PushPop_pp_p6_push_into_hsm_chain__t3 {
    interface:
        bump_f()
        drive()
        get_f(): int
        go_back()
        go_push()

    machine:
        $S0 {
            drive() {
                -> $Child
            }
        }
        $Child => $Parent {
            go_push() {
                push$
                -> $Sibling
            }
            bump_f() {
                self->f = self->f + -1;
            }
            get_f(): int { @@:(self->f) }
        }
        $Sibling {
            go_back() {
                -> pop$
            }
        }
        $Parent {}

    domain:
        f: int = 10
}

#include <stdio.h>
#include <stdlib.h>
int main(void) {
    PushPop_pp_p6_push_into_hsm_chain__t3* _inst = PushPop_pp_p6_push_into_hsm_chain__t3_new();
    PushPop_pp_p6_push_into_hsm_chain__t3_drive(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t3_bump_f(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t3_go_push(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t3_go_back(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t3_bump_f(_inst);
    int _ret = (int)(intptr_t)PushPop_pp_p6_push_into_hsm_chain__t3_get_f(_inst);
    if (_ret != 8) {
        printf("FAIL: expected ret=8, got %d\n", _ret);
        return 1;
    }
    printf("PASS: pushpop\n");
    return 0;
}
