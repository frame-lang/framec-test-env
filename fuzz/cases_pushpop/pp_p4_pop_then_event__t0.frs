@@[target("rust")]


@@system PushPop_pp_p4_pop_then_event__t0 {
    interface:
        drive()
        get_x(): int
        go_back()

    machine:
        $S0 {
            $.x: int = 1
            drive() {
                push$
                -> $S1
            }
            get_x(): int { @@:($.x) }
        }
        $S1 {
            $.x: int = 2
            go_back() {
                -> pop$
            }
        }

    domain:
        f: int = 1
}


fn _fail(msg: &str) -> ! { panic!("FAIL: {}", msg); }

fn main() {
    let mut _inst = PushPop_pp_p4_pop_then_event__t0::new();
    _inst.drive();
    _inst.go_back();
    let _ret = _inst.get_x();
    if _ret != 1 { _fail(&format!("expected ret=1, got {}", _ret)); }
    println!("PASS: pushpop");
}
