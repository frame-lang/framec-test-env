@@target python_3


@@system PushPop_pp_p6_push_into_hsm_chain__t7 {
    interface:
        bump_f()
        drive()
        get_f(): int
        go_back()
        go_push()

    machine:
        $S0 {
            drive() {
                -> $Child
            }
        }
        $Child => $Parent {
            go_push() {
                push$
                -> $Sibling
            }
            bump_f() {
                self.f = self.f + 0
            }
            get_f(): int { @@:(self.f) }
        }
        $Sibling {
            go_back() {
                -> pop$
            }
        }
        $Parent {}

    domain:
        f: int = 1
}


def _fail(msg):
    print(f"FAIL: {msg}")
    raise SystemExit(1)

_inst = PushPop_pp_p6_push_into_hsm_chain__t7()
_inst.drive()
_inst.bump_f()
_inst.go_push()
_inst.go_back()
_inst.bump_f()
_ret = _inst.get_f()
if _ret != 1:
    _fail(f"expected ret=1, got {_ret}")
print("PASS: pushpop")
