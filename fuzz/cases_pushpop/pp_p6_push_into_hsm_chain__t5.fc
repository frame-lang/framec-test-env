@@target c


@@system PushPop_pp_p6_push_into_hsm_chain__t5 {
    interface:
        bump_f()
        drive()
        get_f(): int
        go_back()
        go_push()

    machine:
        $S0 {
            drive() {
                -> $Child
            }
        }
        $Child => $Parent {
            go_push() {
                push$
                -> $Sibling
            }
            bump_f() {
                self->f = self->f + 50;
            }
            get_f(): int { @@:(self->f) }
        }
        $Sibling {
            go_back() {
                -> pop$
            }
        }
        $Parent {}

    domain:
        f: int = 50
}

#include <stdio.h>
#include <stdlib.h>
int main(void) {
    PushPop_pp_p6_push_into_hsm_chain__t5* _inst = PushPop_pp_p6_push_into_hsm_chain__t5_new();
    PushPop_pp_p6_push_into_hsm_chain__t5_drive(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t5_bump_f(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t5_go_push(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t5_go_back(_inst);
    PushPop_pp_p6_push_into_hsm_chain__t5_bump_f(_inst);
    int _ret = (int)(intptr_t)PushPop_pp_p6_push_into_hsm_chain__t5_get_f(_inst);
    if (_ret != 150) {
        printf("FAIL: expected ret=150, got %d\n", _ret);
        return 1;
    }
    printf("PASS: pushpop\n");
    return 0;
}
