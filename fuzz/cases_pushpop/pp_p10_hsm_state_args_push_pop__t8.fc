@@target c


@@system PushPop_pp_p10_hsm_state_args_push_pop__t8 {
    interface:
        drive()
        get_x(): int
        go_back()
        push_then()

    machine:
        $S0 {
            drive() {
                -> $Child(99)
            }
        }
        $Child => $Parent(x: int) {
            push_then() {
                push$
                -> $Sibling
            }
            get_x(): int { @@:(x) }
        }
        $Sibling {
            go_back() {
                -> pop$
            }
        }
        $Parent {}

    domain:
        f: int = 99
}

#include <stdio.h>
#include <stdlib.h>
int main(void) {
    PushPop_pp_p10_hsm_state_args_push_pop__t8* _inst = PushPop_pp_p10_hsm_state_args_push_pop__t8_new();
    PushPop_pp_p10_hsm_state_args_push_pop__t8_drive(_inst);
    PushPop_pp_p10_hsm_state_args_push_pop__t8_push_then(_inst);
    PushPop_pp_p10_hsm_state_args_push_pop__t8_go_back(_inst);
    int _ret = (int)(intptr_t)PushPop_pp_p10_hsm_state_args_push_pop__t8_get_x(_inst);
    if (_ret != 99) {
        printf("FAIL: expected ret=99, got %d\n", _ret);
        return 1;
    }
    printf("PASS: pushpop\n");
    return 0;
}
