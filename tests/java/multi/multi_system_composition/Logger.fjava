@@[target("java")]

@@system Logger {
    interface:
        log(msg: String)

    machine:
        $Active {
            log(msg: String) {
                System.out.println("[LOG] " + msg);
            }
        }
}
