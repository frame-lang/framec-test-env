@@[target("java")]

@@system Counter {
    interface:
        increment()
        get_count(): int

    machine:
        $Active {
            increment() {
                this.count = this.count + 1;
            }

            get_count(): int {
                @@:(this.count)
                return;
            }
        }
    domain:
        count : int = 0
}
