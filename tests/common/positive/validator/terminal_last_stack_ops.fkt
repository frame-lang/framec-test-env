@@[target("kotlin")]

import kotlin.system.exitProcess

@@system S {
    machine:
        $A {
            e() {
                // native prelude
                push$
            }
        }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = S()
        s.e()
        println("ok 1 - terminal_last_stack_ops")
    } catch (ex: Exception) {
        println("not ok 1 - terminal_last_stack_ops # $ex")
    }
}
