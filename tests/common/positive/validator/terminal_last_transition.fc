@@target c

@@system S {
    machine:
        $A {
            e() {
                // native prelude
                -> $B()
            }
        }
        $B {
        }
}

// Stub functions for placeholder calls
void native(void) {}
void x(void) {}

// TAP test harness
int main() {
    printf("TAP version 14\n");
    printf("1..1\n");
    S *s = S_new();
    S_e(s);
    printf("ok 1 - terminal_last_transition\n");
    S_destroy(s);
    return 0;
}
