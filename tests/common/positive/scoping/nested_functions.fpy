@@[target("python")]
@@exec-ok

@@system S {
    machine:
        $A => $P {
            e() {
                def outer():
                    v = 1
                    def inner():
                        return v + 1
                    return inner()
                => $^
                outer()
            }
        }
        $P { }
}

# Stub functions for placeholder calls
def native(): pass
def x(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@S()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - nested_functions")
    except Exception as ex:
        print(f"not ok 1 - nested_functions # {ex}")
