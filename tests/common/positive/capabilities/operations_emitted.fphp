<?php
@@[target("php")]

@@system S {
    operations:
        calc(x, y) { return $x + $y; }

    machine:
        $A {
            e() {
                $this->calc(1, 2);
            }
        }
}

function native() {}
function x() {}

echo "TAP version 14" . "\n";
echo "1..1" . "\n";
try {
    $s = new S();
    if (method_exists($s, 'e')) {
        $s->e();
    }
    echo "ok 1 - operations_emitted" . "\n";
} catch (\Exception $ex) {
    echo "not ok 1 - operations_emitted # " . $ex->getMessage() . "\n";
}
