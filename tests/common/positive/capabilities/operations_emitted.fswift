@@target swift

@@system S {
    operations:
        calc(x, y) { return x + y }
    machine:
        $A {
            e() {
                self.calc(1, 2)
            }
        }
}

// TAP test harness
// main
    print("TAP version 14")
    print("1..1")
    let s = @@S()
    print("ok 1 - operations_emitted")
