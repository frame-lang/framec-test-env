@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A {
            E() {
                s.helper()
            }
        }
    actions:
        helper() { }
}

// Stub functions for placeholder calls
func nativeStub() {}
func x() {}
func helperStub() {}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - actions_emitted")
}
