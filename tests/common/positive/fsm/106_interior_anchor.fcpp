// @@skip: @@fsm removed from the validation plan (owner ruling 2026-07-30); the regex DSL is out of scope for the milestone grid.
@@[target("cpp_17")]

#include <iostream>

// ============================================================================
// FSM-MATRIX 106_interior_anchor
// ============================================================================
// Interior anchor (Pike VM): `$` mid-pattern is unsatisfiable.
// ============================================================================

@@fsm M(text: bytes) : bool = false { /a$b/ true }

int main() {
    M r0("ab");
    if (r0.accepted != false) {
        std::cout << "FAIL: 106_interior_anchor ab" << std::endl;
        return 1;
    }
    std::cout << "PASS: 106_interior_anchor" << std::endl;
    return 0;
}
