@@[target("c")]

#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

// ============================================================================
// FSM-MATRIX 106_interior_anchor
// ============================================================================
// Interior anchor (Pike VM): `$` mid-pattern is unsatisfiable.
// ============================================================================

@@fsm M(text: bytes) : bool = false { /a$b/ true }

int main() {
    struct M r0;
    M_init(&r0, "ab");
    if (r0.accepted != false) {
        printf("FAIL: 106_interior_anchor ab\n");
        return 1;
    }
    printf("PASS: 106_interior_anchor\n");
    return 0;
}
