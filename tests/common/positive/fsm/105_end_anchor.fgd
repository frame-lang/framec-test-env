@@[target("gdscript")]
extends SceneTree

# ============================================================================
# FSM-MATRIX 105_end_anchor
# ============================================================================
# End anchor `[0-9]+$` — the digit run must reach end of input.
# ============================================================================

@@fsm M(text: bytes) : bool = false { /[0-9]+$/ true }

func main():
	var r0 = M.new("123")
	if r0.accepted != true or r0.cursor != 3 or r0.return_value != true:
		push_error("105_end_anchor 123")
		return
	var r1 = M.new("123x")
	if r1.accepted != false:
		push_error("105_end_anchor 123x")
		return
	print("PASS: 105_end_anchor")

func _init():
	main()
	quit()
