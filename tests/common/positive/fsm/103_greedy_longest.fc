// @@skip: @@fsm removed from the validation plan (owner ruling 2026-07-30); the regex DSL is out of scope for the milestone grid.
@@[target("c")]

#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

// ============================================================================
// FSM-MATRIX 103_greedy_longest
// ============================================================================
// Greedy `a*b` longest-match: `a*` consumes all `a`s before `b`.
// ============================================================================

@@fsm M(text: bytes) : bool = false { /a*b/ true }

int main() {
    struct M r0;
    M_init(&r0, "aaab");
    if (r0.accepted != true || r0.cursor != 4 || r0.return_value != true) {
        printf("FAIL: 103_greedy_longest aaab\n");
        return 1;
    }
    struct M r1;
    M_init(&r1, "b");
    if (r1.accepted != true || r1.cursor != 1 || r1.return_value != true) {
        printf("FAIL: 103_greedy_longest b\n");
        return 1;
    }
    struct M r2;
    M_init(&r2, "aaa");
    if (r2.accepted != false) {
        printf("FAIL: 103_greedy_longest aaa\n");
        return 1;
    }
    printf("PASS: 103_greedy_longest\n");
    return 0;
}
