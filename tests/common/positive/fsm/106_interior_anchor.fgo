@@[target("go")]

package main

import "fmt"
import "os"

// ============================================================================
// FSM-MATRIX 106_interior_anchor
// ============================================================================
// Interior anchor (Pike VM): `$` mid-pattern is unsatisfiable.
// ============================================================================

@@fsm M(text: bytes) : bool = false { /a$b/ true }

func main() {
    r0 := NewM("ab")
    if r0.accepted != false {
        fmt.Println("FAIL: 106_interior_anchor ab")
        os.Exit(1)
    }
    fmt.Println("PASS: 106_interior_anchor")
}
