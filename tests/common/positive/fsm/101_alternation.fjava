@@[target("java")]

// ============================================================================
// FSM-MATRIX 101_alternation
// ============================================================================
// Alternation `cat|dog` — first-match-wins (RE2 leftmost).
// ============================================================================

@@fsm M(text: bytes) : bool = false { /cat|dog/ true }

class Main {
    public static void main(String[] args) {
        M r0 = new M("cat");
        if (r0.accepted != true || r0.cursor != 3 || r0.return_value != true) {
            throw new RuntimeException("101_alternation cat");
        }
        M r1 = new M("dog");
        if (r1.accepted != true || r1.cursor != 3 || r1.return_value != true) {
            throw new RuntimeException("101_alternation dog");
        }
        M r2 = new M("cow");
        if (r2.accepted != false) {
            throw new RuntimeException("101_alternation cow");
        }
        System.out.println("PASS: 101_alternation");
    }
}
