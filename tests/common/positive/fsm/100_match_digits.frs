// @@skip: @@fsm removed from the validation plan (owner ruling 2026-07-30); the regex DSL is out of scope for the milestone grid.
@@[target("rust")]

// =============================================================================
// FSM-MATRIX 100: match digits — core DFA recognizer
// =============================================================================
// `/[0-9]+/` accepts a digit run at the cursor. "123" matches (cursor=3);
// "xyz" rejects. Bytes input is a Vec<char> in the Rust backend.
// =============================================================================

@@fsm Digits(text: bytes) : bool = false { /[0-9]+/ true }

fn main() {
    let ok = Digits::new("123".chars().collect());
    assert_eq!(ok.accepted, true);
    assert_eq!(ok.cursor, 3);
    assert_eq!(ok.return_value, true);
    let bad = Digits::new("xyz".chars().collect());
    assert_eq!(bad.accepted, false);
    println!("PASS: 100_match_digits");
}
