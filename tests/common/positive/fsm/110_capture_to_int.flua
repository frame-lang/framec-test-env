// @@skip: @@fsm removed from the validation plan (owner ruling 2026-07-30); the regex DSL is out of scope for the milestone grid.
@@[target("lua")]

-- ============================================================================
-- FSM-MATRIX 110_capture_to_int
-- ============================================================================
-- Stage capture `.n` + to_int($s.n) → return_value is the integer.
-- ============================================================================

@@fsm M(text: bytes) : int = 0 { $s: .n/[0-9]+/ to_int($s.n) }

local r0 = M.new("42")
assert(r0.accepted == true, "110_capture_to_int 42: accepted")
assert(r0.cursor == 2, "110_capture_to_int 42: cursor")
assert(r0.return_value == 42, "110_capture_to_int 42: return_value")
local r1 = M.new("x")
assert(r1.accepted == false, "110_capture_to_int x: accepted")
print("PASS: 110_capture_to_int")
