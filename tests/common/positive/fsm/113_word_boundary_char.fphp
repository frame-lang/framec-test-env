// @@skip — `@@fsm` removed from the validation plan (owner ruling 2026-07-30); the regex DSL is out of scope for the milestone grid.
@@[target("php")]

# ============================================================================
# FSM-MATRIX 113_word_boundary_char
# ============================================================================
# Word boundary `\bcat\b` on the `char` alphabet (Pike VM with the Unicode `\w` word table).
# ============================================================================

@@fsm M(text: char) : bool = false { /\bcat\b/ true }

$r0 = new M("cat");
if ($r0->accepted !== true || $r0->cursor !== 3 || $r0->return_value !== true) {
    echo "FAIL: 113_word_boundary_char cat\n";
    throw new \Exception("113_word_boundary_char cat");
}
$r1 = new M("cats");
if ($r1->accepted !== false) {
    echo "FAIL: 113_word_boundary_char cats\n";
    throw new \Exception("113_word_boundary_char cats");
}
echo "PASS: 113_word_boundary_char\n";
