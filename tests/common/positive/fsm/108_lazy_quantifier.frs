@@[target("rust")]

// ============================================================================
// FSM-MATRIX 108_lazy_quantifier
// ============================================================================
// Lazy `.*?,` (Pike VM): stops at the FIRST comma; matched="ab,".
// ============================================================================

@@fsm M(text: bytes) : bytes = "" { /.*?,/ @@:matched }

fn main() {
    let r0 = M::new("ab,cd,ef".chars().collect());
    assert_eq!(r0.accepted, true);
    assert_eq!(r0.cursor, 3);
    assert_eq!(r0.return_value, "ab,".to_string());
    println!("PASS: 108_lazy_quantifier");
}
