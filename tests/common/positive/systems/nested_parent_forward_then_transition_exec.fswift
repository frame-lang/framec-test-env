@@[target("swift")]
@@run-expect: FORWARD:PARENT
@@run-expect: TRANSITION:

@@system S {
    machine:
        $A => $P {
            e() {
                if (true) {
                    => $^; let x = 1
                    if (x == 1) {
                        -> $B()
                    }
                }
            }
        }
        $B { }
        $P { }
}

// TAP test harness
// main
    print("TAP version 14")
    print("1..1")
    let s = @@S()
    print("ok 1 - nested_parent_forward_then_transition_exec")
