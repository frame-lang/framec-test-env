@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A => $P {
            E() {
                // nested structure with inline separators
                if true {
                    => $^; x := 1
                    if x == 1 {
                        -> $B()
                    }
                }
            }
        }
        $B { }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - nested_parent_forward_then_transition_exec")
}
