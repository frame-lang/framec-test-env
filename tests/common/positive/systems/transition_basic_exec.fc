@@[target("c")]
@@run-expect: TRANSITION:

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

@@system S {
    machine:
        $A {
            e() {
                -> $B()
            }
        }
        $B { }
}

int main() {
    S* s = @@S();
    S_e(s);
    S_destroy(s);
    printf("PASS\n");
    return 0;
}
