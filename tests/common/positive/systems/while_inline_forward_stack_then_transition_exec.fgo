@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $P { }
        $A => $P {
            E() {
                i := 0
                for i < 2 {
                    if i == 0 {
                        push$
                        => $^
                    }
                    i = i + 1
                }
                -> $B()
            }
        }
        $B { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - while_inline_forward_stack_then_transition_exec")
}
