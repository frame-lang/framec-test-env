@@target go

package main

import "fmt"

func nativeStub() {}

@@system S {
    machine:
        $A {
            E() {
                // comment with -> $B() and => $^
                nativeStub()
            }
        }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - inside_comment_transition_ignored")
}
