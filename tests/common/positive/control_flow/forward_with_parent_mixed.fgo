@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $P {
        }
        $A => $P {
            E() {
                => $^
            }
        }
        $B {
        }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - forward_with_parent_mixed")
}
