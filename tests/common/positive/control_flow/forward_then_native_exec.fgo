@@[target("go")]

package main

import "fmt"

func nativeStub() {}

@@system S {
    machine:
        $A => $P {
            E() {
                => $^; nativeStub()
            }
        }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - forward_then_native_exec")
}
