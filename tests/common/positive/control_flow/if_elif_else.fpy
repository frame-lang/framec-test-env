@@[target("python")]

@@system S {
    machine:
        $A => $P {
            e() {
                if a:
                    x()
                elif b:
                    y()
                else:
                    z()
                => $^
            }
        }
        $P { }
}

# Stub variables and functions for placeholder calls
a = True
b = False
def native(): pass
def x(): pass
def y(): pass
def z(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@S()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - if_elif_else")
    except Exception as ex:
        print(f"not ok 1 - if_elif_else # {ex}")
