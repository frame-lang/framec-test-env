@@[target("javascript")]

@@system S {
    machine:
        $A => $P {
            e() {
                // native prelude
                => $^
            }
        }
        $P { }
}

function native() {}
function x() {}

function main() {
    console.log("TAP version 14");
    console.log("1..1");
    try {
        const s = @@S();
        if (typeof s.e === 'function') { s.e(); }
        console.log("ok 1 - terminal_last_forward");
    } catch (ex) {
        console.log(`not ok 1 - terminal_last_forward # ${ex}`);
    }
}
main();
