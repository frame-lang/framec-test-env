@@target go

package main

import "fmt"

@@system S {
    machine:
        $A {
            E(p, q) { -> $B() }
            G(x) { ; }
        }
        $B { E() { } }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E(0, 0)
	fmt.Println("ok 1 - multi_handler_manifest")
}
