@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A {
            E(p: int, q: int) { -> $B() }
            G(x: int) { ; }
        }
        $B { E() { } }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E(0, 0)
	fmt.Println("ok 1 - multi_handler_manifest")
}
