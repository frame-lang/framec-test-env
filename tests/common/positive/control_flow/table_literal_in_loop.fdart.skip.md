# Intentional skip — Lua-specific table literal

Lua-specific: exercises a Lua table literal (`{...}`) inside a loop body — native Lua syntax with no cross-language analogue. Single-target by design.

See `docs/partial-coverage-audit.md`.
