@@[target("go")]

package main

import "fmt"

@@system SysX {
    machine:
        $A {
            E() {
                -> $B();
            }
        }
        $B {
        }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewSysX()
	s.E()
	fmt.Println("ok 1 - transition_state_id_exec")
}
