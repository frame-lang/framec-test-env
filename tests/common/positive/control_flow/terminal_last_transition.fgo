@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A {
            E() {
                // native prelude
                -> $B()
            }
        }
        $B {
        }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - terminal_last_transition")
}
