@@[target("swift")]

@@system S {
    machine:
        $A => $P {
            e() {
                while (cond) {
                    => $^
                    step()
                }
                done()
            }
        }
        $P { }
}

func done() {}
func step() {}
let cond = false

// main
    print("TAP version 14")
    print("1..1")
    let s = @@S()
    print("ok 1 - while_forward_then_native")
