@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A {
            E() {
                if false {
                    -> $B()
                } else if false {
                    -> $C()
                } else {
                    -> $D()
                }
            }
        }
        $B { }
        $C { }
        $D { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - elif_multi_transition_exec")
}
