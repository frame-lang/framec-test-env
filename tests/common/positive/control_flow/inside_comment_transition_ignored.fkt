@@target kotlin

import kotlin.system.exitProcess

@@system S {
    machine:
        $A {
            e() {
                // comment with -> $B() and => $^
            }
        }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = S()
        s.e()
        println("ok 1 - inside_comment_transition_ignored")
    } catch (ex: Exception) {
        println("not ok 1 - inside_comment_transition_ignored # $ex")
    }
}
