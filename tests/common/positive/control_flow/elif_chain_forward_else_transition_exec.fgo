@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A => $P {
            E() {
                v := 2
                if v == 1 {
                    -> $B()
                } else if v == 2 {
                    => $^
                } else {
                    -> $B()
                }
            }
        }
        $B { }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - elif_chain_forward_else_transition_exec")
}
