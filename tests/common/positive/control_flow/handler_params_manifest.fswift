@@[target("swift")]
@@debug-manifest-expect: system=S; states=A,B
@@debug-manifest-handler-expect: state=A; name=e; params=x,y

@@system S {
    machine:
        $A {
            e(x, y) {
                -> $B()
            }
        }
        $B { e() { } }
}

// main
    print("TAP version 14")
    print("1..1")
    let s = @@S()
    print("ok 1 - handler_params_manifest")
