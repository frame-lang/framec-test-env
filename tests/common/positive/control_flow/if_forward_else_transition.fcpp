@@[target("cpp_17")]

#include <iostream>
#include <string>
#include <cassert>

@@system S {
    machine:
        $P { e() { } }
        $A => $P {
            e() {
                if (true) {
                    => $^
                } else {
                    -> $B()
                }
            }
        }
        $B { e() { } }
}

// Stub functions for placeholder calls
void native_func() {}
void x_func() {}

// TAP test harness
int main() {
    printf("TAP version 14\n");
    printf("1..1\n");
    try {
        S s;
        s.e();
        printf("ok 1 - if_forward_else_transition\n");
    } catch (...) {
        printf("not ok 1 - if_forward_else_transition\n");
    }
    return 0;
}
