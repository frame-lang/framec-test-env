@@[target("swift")]

@@system S {
    machine:
        $P { e() { } }
        $A => $P {
            e() {
                if (true) {
                    => $^
                } else {
                    -> $B()
                }
            }
        }
        $B { e() { } }
}

// main
    print("TAP version 14")
    print("1..1")
    let s = @@S()
    print("ok 1 - if_forward_else_transition")
