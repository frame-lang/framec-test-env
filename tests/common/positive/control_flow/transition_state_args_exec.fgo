@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A {
            E() {
                -> $B(1, 2, 3)
            }
        }
        $B(x, y, z) { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - transition_state_args_exec")
}
