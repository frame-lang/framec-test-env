@@target go

package main

import "fmt"

@@system S {
    machine:
        $A => $P {
            E() {
                push$
                => $^
                -> $B()
            }
        }
        $B { }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - stack_forward_then_transition_exec")
}
