@@[target("c")]
@@run-expect: TRANSITION:__S_state_B

#include <stdio.h>
#include <stdbool.h>

@@system S {
    machine:
        $A {
            e() {
                -> $B(1, 2, 3)
            }
        }
        $B(x, y, k) { }
}

// Stub functions for placeholder calls
void native(void) {}
void x(void) {}

// TAP test harness
int main(void) {
    printf("TAP version 14\n");
    printf("1..1\n");
    S* s = @@S();
    S_e(s);
    printf("ok 1 - transition_state_args_exec\n");
    S_destroy(s);
    return 0;
}
