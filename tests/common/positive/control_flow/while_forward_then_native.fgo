@@[target("go")]

package main

import "fmt"

var cond = false

func step() {}
func done() {}

@@system S {
    machine:
        $A => $P {
            E() {
                for cond {
                    => $^
                    step()
                }
                done()
            }
        }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - while_forward_then_native")
}
