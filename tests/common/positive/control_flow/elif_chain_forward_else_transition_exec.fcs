@@[target("csharp")]

using System;
using System.Collections.Generic;

@@system S {
    machine:
        $A => $P {
            e() {
                int v = 2;
                if (v == 1) {
                    -> $B()
                } else if (v == 2) {
                    => $^
                } else {
                    -> $B()
                }
            }
        }
        $B { }
        $P { }
}

class Program {
    static void Main(string[] args) {
        Console.WriteLine("TAP version 14");
        Console.WriteLine("1..1");
        try {
            S s = new S();
            s.e();
            Console.WriteLine("ok 1 - elif_chain_forward_else_transition_exec");
        } catch (Exception ex) {
            Console.WriteLine("not ok 1 - elif_chain_forward_else_transition_exec # " + ex);
        }
    }
}
