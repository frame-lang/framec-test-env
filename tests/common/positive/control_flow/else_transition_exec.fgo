@@[target("go")]

package main

import "fmt"

func nativeStub() {}

@@system S {
    machine:
        $A {
            E() {
                if false {
                    nativeStub()
                } else {
                    -> $B()
                }
            }
        }
        $B { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - else_transition_exec")
}
