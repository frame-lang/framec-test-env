@@target go

package main

import "fmt"

@@system S {
    machine:
        $A {
            E() {
                if true {
                    if true {
                        -> $B()
                    }
                }
            }
        }
        $B { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - nested_if_transition_exec")
}
