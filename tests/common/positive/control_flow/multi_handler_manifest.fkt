@@[target("kotlin")]

import kotlin.system.exitProcess

@@system S {
    machine:
        $A {
            e(p, q) { -> $B() }
            g(x) { }
        }
        $B { e() { } }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = S()
        s.e(0, 0)
        println("ok 1 - multi_handler_manifest")
    } catch (ex: Exception) {
        println("not ok 1 - multi_handler_manifest # $ex")
    }
}
