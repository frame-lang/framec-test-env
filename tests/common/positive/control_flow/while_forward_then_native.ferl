@@target erlang
@@skip -- Erlang has no while loop construct (functional language uses recursion)@@system S {
    machine:
        $A => $P {
            e() {
                while cond {
                    => $^
                    step()
                }
                done()
            }
        }
        $P { }
}
