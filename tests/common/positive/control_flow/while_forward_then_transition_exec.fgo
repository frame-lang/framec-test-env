@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A => $P {
            E() {
                i := 0
                for i < 1 {
                    => $^
                    i += 1
                }
                -> $B()
            }
        }
        $B { }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - while_forward_then_transition_exec")
}
