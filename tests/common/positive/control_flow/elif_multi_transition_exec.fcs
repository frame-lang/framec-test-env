@@[target("csharp")]

using System;
using System.Collections.Generic;

@@system S {
    machine:
        $A {
            e() {
                if (false) {
                    -> $B()
                } else if (false) {
                    -> $C()
                } else {
                    -> $D()
                }
            }
        }
        $B { }
        $C { }
        $D { }
}

class Program {
    static void Main(string[] args) {
        Console.WriteLine("TAP version 14");
        Console.WriteLine("1..1");
        try {
            S s = @@S();
            s.e();
            Console.WriteLine("ok 1 - elif_multi_transition_exec");
        } catch (Exception ex) {
            Console.WriteLine("not ok 1 - elif_multi_transition_exec # " + ex);
        }
    }
}
