@@[target("swift")]

@@system S {
    machine:
        $A {
            e() {
                if (true) {
                    push$
                    pop$
                } else {
                    push$
                    pop$
                -> $B()
                }
            }
        }
        $B { e() { } }
}

// main
    print("TAP version 14")
    print("1..1")
    let s = @@S()
    print("ok 1 - nested_stack_then_transition")
