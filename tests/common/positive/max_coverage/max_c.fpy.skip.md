# Intentional skip — per-language maximal-surface fixture

Per-language **maximal-surface** fixture: exercises the fullest Frame feature set a single backend supports (HSM, stack, all statement forms, transitions, references, const, actions, operations, multi-system, persist, async) written in that backend's own idioms. Single-target by design — there is no cross-language equivalent; broad behavioral parity is covered by the per-feature suites.

See `docs/partial-coverage-audit.md`.
