<?php
@@[target("php")]

@@system S2 {
    machine:
        $A {
            e() { -> $A() }
        }
}

function native() {}
function x() {}

echo "TAP version 14" . "\n";
echo "1..1" . "\n";
try {
    $s = @@S2();
    if (method_exists($s, 'e')) { $s->e(); }
    echo "ok 1 - mod_b" . "\n";
} catch (\Exception $ex) {
    echo "not ok 1 - mod_b # " . $ex->getMessage() . "\n";
}
