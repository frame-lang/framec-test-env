@@target kotlin

import kotlin.system.exitProcess

@@system S {
    machine:
        $A {
            e() {
                push$
                -> $B()
            }
        }
        $B { e() { } }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = S()
        s.e()
        println("ok 1 - mixed_ops")
    } catch (ex: Exception) {
        println("not ok 1 - mixed_ops # $ex")
    }
}
