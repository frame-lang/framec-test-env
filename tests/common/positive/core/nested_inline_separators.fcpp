@@[target("cpp_17")]
@@visitor-map-golden: origins=frame; min=1

#include <iostream>
#include <string>
#include <cassert>

@@system S {
    machine:
        $A {
            e() {
                if (true) {
                    int x = 1; int y = 2;  // inline sep
                } else {
                    int y = 3; int x = 4;  // inline sep
                    -> $B()
                }
            }
        }
        $B { e() { } }
}

// Stub functions for placeholder calls
void native() {}
void x() {}

// TAP test harness
int main() {
    printf("TAP version 14\n");
    printf("1..1\n");
    try {
        auto s = @@S();
        s.e();
        printf("ok 1 - nested_inline_separators\n");
    } catch (...) {
        printf("not ok 1 - nested_inline_separators\n");
    }
    return 0;
}
