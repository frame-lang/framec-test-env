@@[target("ruby")]

@@system S2 {
    machine:
        $A {
            e() { -> $A() }
        }
}

puts("TAP version 14")
puts("1..1")
begin
    s = S2.new
    if s.respond_to?(:e)
        s.e()
    end
    puts("ok 1 - mod_b")
rescue => ex
    puts("not ok 1 - mod_b # #{ex}")
end
