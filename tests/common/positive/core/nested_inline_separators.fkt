@@[target("kotlin")]

import kotlin.system.exitProcess

@@system S {
    machine:
        $A {
            e() {
                if (true) {
                    val x = 1; val y = 2  // inline sep
                } else {
                    val y = 3; val x = 4  // inline sep
                    -> $B()
                }
            }
        }
        $B { e() { } }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = @@S()
        s.e()
        println("ok 1 - nested_inline_separators")
    } catch (ex: Exception) {
        println("not ok 1 - nested_inline_separators # $ex")
    }
}
