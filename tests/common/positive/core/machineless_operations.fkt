@@[target("kotlin")]
@@system Calc {
    operations:
        add(a: Int, b: Int): Int { return a + b; }
}

fun main() {
    println("TAP version 14"); println("1..1")
    val m = @@Calc()
    if (m.add(2, 3) == 5) println("ok 1 - machineless add") else println("not ok 1 - add # got ${m.add(2, 3)}")
}
