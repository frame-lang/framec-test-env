@@target javascript
@@visitor-map-golden: origins=frame; min=1

@@system S {
    machine:
        $A {
            e() {
                if (true) {
                    let x = 1; let y = 2  // inline sep
                } else {
                    let y = 3; let x = 4  // inline sep
                    -> $B()
                }
            }
        }
        $B { e() { } }
}

function native() {}
function x() {}

function main() {
    console.log("TAP version 14");
    console.log("1..1");
    try {
        const s = @@S();
        if (typeof s.e === 'function') { s.e(); }
        console.log("ok 1 - nested_inline_separators");
    } catch (ex) {
        console.log(`not ok 1 - nested_inline_separators # ${ex}`);
    }
}
main();
