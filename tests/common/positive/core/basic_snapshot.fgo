@@[target("go")]

package main

import "fmt"

func nativeStub() {}

@@system S {
    machine:
        $A {
            E(x, y, z) {
                nativeStub()
            }
        }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E("a", "b", "c")
	fmt.Println("ok 1 - basic_snapshot")
}
