@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A => $P {
            Ev() {
                => $^
            }
        }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	_ = @@S()
	fmt.Println("ok 1 - simple_interface")
}
