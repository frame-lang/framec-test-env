@@[target("ruby")]
@@system Calc {
    operations:
        add(a: int, b: int): int { return a + b; }
}

puts "TAP version 14"; puts "1..1"
m = @@Calc()
puts(m.add(2, 3) == 5 ? "ok 1 - machineless add" : "not ok 1 - add # got #{m.add(2, 3)}")
