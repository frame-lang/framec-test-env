@@[target("ruby")]

@@system S {
    machine:
        $P { }
}

puts("TAP version 14")
puts("1..1")
begin
    s = @@S()
    if s.respond_to?(:e)
        s.e()
    end
    puts("ok 1 - parent")
rescue => ex
    puts("not ok 1 - parent # #{ex}")
end
