@@[target("kotlin")]

import kotlin.system.exitProcess

@@system S {
    machine:
        $A {
            e(x, y, z) {
            }
        }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = @@S()
        s.e("a", "b", "c")
        println("ok 1 - basic_snapshot")
    } catch (ex: Exception) {
        println("not ok 1 - basic_snapshot # $ex")
    }
}
