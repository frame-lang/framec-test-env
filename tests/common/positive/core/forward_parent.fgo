@@[target("go")]

package main

import "fmt"

func x() {}

@@system S {
    machine:
        $A => $P {
            E() {
                => $^
                x()
            }
        }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - forward_parent")
}
