@@[target("rust")]

use std::collections::HashMap;

@@system S {
    machine:
        $A => $P {
            e() {
                let mut d: HashMap<String, i32> = HashMap::new();
                d.insert("k".to_string(), 1);
                d.insert("k".to_string(), 2);
                => $^
                d.get("k");
            }
        }
        $P { }
}

fn main() {
    println!("TAP version 14");
    println!("1..1");
    let mut s = @@S();
    s.e();
    println!("ok 1 - dict_ops");
}
