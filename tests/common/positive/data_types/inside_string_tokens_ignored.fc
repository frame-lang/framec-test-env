@@target c

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Forward declaration for stub
void native(void);

@@system S {
    machine:
        $A {
            e() {
                char* s = "-> $B() and => $^ ignored";
                native();
            }
        }
}

// Stub function
void native(void) {}

// TAP test harness
int main() {
    printf("TAP version 14\n");
    printf("1..1\n");

    S* s = @@S();
    if (s != NULL) {
        S_e(s);
        printf("ok 1 - inside_string_tokens_ignored\n");
        S_destroy(s);
    } else {
        printf("not ok 1 - inside_string_tokens_ignored # failed to create system\n");
        return 1;
    }

    return 0;
}
