@@target python

@@system S {
    machine:
        $A => $P {
            e() {
                a = [1,2]
                a.append(3)
                => $^
                len(a)
            }
        }
        $P { }
}

# Stub functions for placeholder calls
def native(): pass
def x(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@S()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - list_ops")
    except Exception as ex:
        print(f"not ok 1 - list_ops # {ex}")
