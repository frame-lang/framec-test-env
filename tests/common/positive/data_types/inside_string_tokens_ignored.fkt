@@[target("kotlin")]

import kotlin.system.exitProcess

@@system S {
    machine:
        $A {
            e() {
                val s = "-> \$B() and => \$^ ignored"
            }
        }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = @@S()
        s.e()
        println("ok 1 - inside_string_tokens_ignored")
    } catch (ex: Exception) {
        println("not ok 1 - inside_string_tokens_ignored # $ex")
    }
}
