@@[target("javascript")]

@@system MixedBodyStringsComments {
    machine:
        $Init {
            start() {
                // Native JavaScript with Frame-statement-like tokens in strings and comments
                const text = "This mentions -> $Next and pop$ inside a string.";
                // A comment that mentions => $^ and -> $Other should not be parsed as Frame
                console.log(text);
                -> $Done
            }
        }
        $Done {}
}

function main() {
    console.log("TAP version 14");
    console.log("1..1");
    try {
        const s = @@MixedBodyStringsComments();
        s.start();
        console.log("ok 1 - test_mixed_body_strings_comments");
    } catch (ex) {
        console.log(`not ok 1 - test_mixed_body_strings_comments # ${ex}`);
    }
}
main();
