@@[target("go")]

package main

import "fmt"

@@system S {
    machine:
        $A => $P {
            E() {
                d := make(map[string]int)
                d["k"] = 1
                d["k"] = 2
                => $^
                _ = d["k"]
            }
        }
        $P { }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := @@S()
	s.E()
	fmt.Println("ok 1 - dict_ops")
}
