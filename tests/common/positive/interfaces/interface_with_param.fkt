@@[target("kotlin")]

import kotlin.system.exitProcess

@@system S {
    machine:
        $A => $P {
            ev(n: Int) {
                => $^
                n.toString()
            }
        }
        $P { }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = S()
        println("ok 1 - interface_with_param")
    } catch (ex: Exception) {
        println("not ok 1 - interface_with_param # $ex")
    }
}
