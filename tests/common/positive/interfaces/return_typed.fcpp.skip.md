# Intentional skip — TypeScript-specific edge case

TypeScript-only fixture for a TS-parser quirk in typed return-value handling. Not a feature test — a regression net for one specific TS codegen path.

See `docs/partial-coverage-audit.md`.
