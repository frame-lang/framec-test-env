@@target go

package main

import "fmt"

@@system S {
    interface:
        E()
    machine:
        $A {
            E() { ; }
        }
    actions:
        doThing() { ; }
}

func main() {
	fmt.Println("TAP version 14")
	fmt.Println("1..1")
	s := NewS()
	s.E()
	fmt.Println("ok 1 - interface_handlers_emitted")
}
