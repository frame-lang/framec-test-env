@@[target("kotlin")]

import kotlin.system.exitProcess

@@system S {
    interface:
        e()
    machine:
        $A {
            e() { }
        }
    actions:
        doThing() { }
}

fun main() {
    println("TAP version 14")
    println("1..1")
    try {
        val s = @@S()
        s.e()
        println("ok 1 - interface_handlers_emitted")
    } catch (ex: Exception) {
        println("not ok 1 - interface_handlers_emitted # $ex")
    }
}
