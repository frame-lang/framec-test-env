@@[target("javascript")]

@@system S {
    interface:
        e()
    machine:
        $A {
            e() { ; }
        }
    actions:
        doThing() { ; }
}

// Stub functions for placeholder calls
function native() {}
function x() {}

// TAP test harness
function main() {
    console.log("TAP version 14");
    console.log("1..1");
    try {
        const s = @@S();
        if (typeof s.e === 'function') {
            s.e();
        }
        console.log("ok 1 - interface_handlers_emitted");
    } catch (ex) {
        console.log(`not ok 1 - interface_handlers_emitted # ${ex}`);
    }
}
main();
