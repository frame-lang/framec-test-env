@@target lua
@@system S {
    interface:
        e()
    machine:
        $A {
            e() { pass }
        }
    actions:
        doThing() { pass }
}

-- Stub functions for placeholder calls
def native(): pass
def x(): -- empty -- TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@S()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - interface_handlers_emitted")
    except Exception as ex:
        print(f"not ok 1 - interface_handlers_emitted # {ex}")
