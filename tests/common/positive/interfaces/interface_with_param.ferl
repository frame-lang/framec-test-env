@@[target("erlang")]
@@system InterfaceParam {
    interface:
        greet(name: str): str
    machine:
        $Active {
            greet(name: str): str {
                @@:("hello")
            }
        }
}
