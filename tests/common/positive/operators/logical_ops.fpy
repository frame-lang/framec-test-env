@@[target("python")]

@@system S {
    machine:
        $A => $P {
            e() {
                if a and b:
                    => $^
                elif a or b:
                    => $^
                else:
                    => $^
            }
        }
        $P { }
}

# Stub variables and functions for placeholder calls
a = 1
b = 2
def native(): pass
def x(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@S()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - logical_ops")
    except Exception as ex:
        print(f"not ok 1 - logical_ops # {ex}")
