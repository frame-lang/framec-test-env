@@target erlang
@@system LogicalOps {
    interface:
        check(a: bool, b: bool): bool
    machine:
        $Active {
            check(a: bool, b: bool): bool {
                if a andalso b {
                    return true
                } else {
                    return false
                }
            }
        }
}