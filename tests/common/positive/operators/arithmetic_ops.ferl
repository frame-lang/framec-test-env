@@[target("erlang")]
@@system ArithOps {
    interface:
        compute(a: int, b: int): int
    machine:
        $Active {
            compute(a: int, b: int): int {
                @@:(a + b * 2)
            }
        }
}
