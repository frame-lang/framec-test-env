// Self-Calibrating Sensor — @@:self demo (C)
// @@skip: C does not support @@:self

#include <stdio.h>

@@system Sensor {
    interface:
        status(): str

    machine:
        $Active {
            status(): str { @@:("active") }
        }
}

int main() {
    printf("SKIP: self_calibrating_sensor — C does not support @@:self\n");
    return 0;
}
