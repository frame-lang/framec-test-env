@@target java
@@skip -- requires multi-system (ToolRunner + Agent)

@@system AiAgent {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}

class Main {
    public static void main(String[] args) {
        System.out.println("SKIP: ai_agent — requires multi-system (ToolRunner + Agent)");
    }
}
