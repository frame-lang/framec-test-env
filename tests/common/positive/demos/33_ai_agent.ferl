@@target erlang
@@skip -- requires multi-system (ToolRunner + Agent)

@@system AiAgent {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}
