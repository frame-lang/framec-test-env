@@target erlang
@@skip -- Erlang system params with all three groups not yet tested

@@system WorkerPool($(max_retries: int), pool_size: int) {
    interface:
        submit()
        get_status()

    machine:
        $Idle(max_retries: int) {
            submit() { }
            get_status() { }
        }
}
