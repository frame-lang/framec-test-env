@@target dart
@@skip -- Dart persistence codegen not yet implemented

@@system Session {
    interface:
        login(user: String)
        logout()
        who(): String

    machine:
        $LoggedOut {
            login(user: String) {
                self.user = user;
                -> $LoggedIn
            }
            who(): String { @@:("nobody") }
        }
        $LoggedIn {
            logout() {
                self.user = "";
                -> $LoggedOut
            }
            who(): String { @@:(self.user) }
        }

    domain:
        user: String = ""
}

void main() {
    print("SKIP: session_persistence -- Dart persistence not yet implemented");
}
