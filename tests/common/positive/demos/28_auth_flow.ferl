@@target erlang
@@skip -- requires multi-system (LoginManager + App)

@@system AuthFlow {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}
