@@target erlang
@@skip -- Erlang requires one module per file; multi-system tested via other targets

@@system AuthFlow {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}
