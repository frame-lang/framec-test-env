<?php
// AI Agent — multi-system + HSM + self-calls (too complex)
@@skip -- requires multi-system + HSM + self-calls combination

@@system AiAgent {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}
