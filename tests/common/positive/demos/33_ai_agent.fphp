<?php
// AI Agent — multi-system + HSM + self-calls (too complex)

@@system AiAgent {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}
