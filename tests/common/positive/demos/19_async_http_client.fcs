@@target csharp
@@skip -- C# async init not yet implemented in Frame

using System;

@@system AsyncWorker {
    interface:
        start()
        get_status(): String

    machine:
        $Idle {
            start() { -> $Working }
            get_status(): String { @@:("idle") }
        }
        $Working {
            get_status(): String { @@:("working") }
        }
}

class Program {
    static void Main(string[] args) {
        Console.WriteLine("SKIP: async_http_client -- C# async init not yet implemented");
    }
}
