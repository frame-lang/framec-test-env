// Self-Calibrating Sensor — @@:self demo (C++)
// @@skip: C++ @@:self not yet supported

#include <iostream>

@@system Sensor {
    interface:
        status(): str

    machine:
        $Active {
            status(): str { @@:("active") }
        }
}

int main() {
    std::cout << "SKIP: self_calibrating_sensor — C++ @@:self not yet supported" << std::endl;
    return 0;
}
