@@target cpp_17
@@skip -- C++ async codegen not yet implemented in Frame

#include <cstdio>

@@system AsyncWorker {
    interface:
        start()
        get_status(): str

    machine:
        $Idle {
            start() { -> $Working }
            get_status(): str { @@:(std::string("idle")) }
        }
        $Working {
            get_status(): str { @@:(std::string("working")) }
        }
}

int main() {
    printf("SKIP: async_http_client -- C++ async codegen not yet implemented\n");
    return 0;
}
