%% Self-Calibrating Sensor — @@:self demo (Erlang)
%% @@skip: Erlang @@:self not yet supported

-module(self_calibrating_sensor).
-export([main/0]).

@@system Sensor {
    interface:
        status(): str

    machine:
        $Active {
            status(): str { @@:("active") }
        }
}

main() ->
    io:format("SKIP: self_calibrating_sensor — Erlang @@:self not yet supported~n").
