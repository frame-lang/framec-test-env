@@target erlang
@@skip -- service pattern with kernel loop doesn't work for Erlang test runner

@@system GracefulShutdown {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}
