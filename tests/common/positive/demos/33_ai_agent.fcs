@@target csharp
@@skip -- requires multi-system (ToolRunner + Agent)

using System;

@@system AiAgent {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}

class Program {
    static void Main(string[] args) {
        Console.WriteLine("SKIP: ai_agent — requires multi-system (ToolRunner + Agent)");
    }
}
