@@target c
@@skip -- requires multi-system (ToolRunner + Agent)

#include <stdio.h>

@@system AiAgent {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}

int main() {
    printf("SKIP: ai_agent — requires multi-system (ToolRunner + Agent)\n");
    return 0;
}
