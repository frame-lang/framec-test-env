@@[target("gdscript")]

extends SceneTree

@@system Turnstile {
    interface:
        coin()
        push(): String

    machine:
        $Locked {
            coin() { -> $Unlocked }
            push(): String { @@:("locked - insert coin") }
        }
        $Unlocked {
            coin() { }
            push(): String {
                @@:("welcome")
                -> $Locked
            }
        }
}

func _init():
    var t = @@Turnstile()
    assert(t.push() == "locked - insert coin", "Expected 'locked - insert coin'")
    t.coin()
    assert(t.push() == "welcome", "Expected 'welcome'")
    assert(t.push() == "locked - insert coin", "Expected 'locked - insert coin'")
    print("PASS: turnstile")
    quit()
