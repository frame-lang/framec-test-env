@@target dart
@@skip -- Dart async init not yet implemented in Frame

@@system AsyncWorker {
    interface:
        start()
        get_status(): String

    machine:
        $Idle {
            start() { -> $Working }
            get_status(): String { @@:("idle") }
        }
        $Working {
            get_status(): String { @@:("working") }
        }
}

void main() {
    print("SKIP: async_http_client -- Dart async init not yet implemented");
}
