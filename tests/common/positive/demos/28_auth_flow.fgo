@@target go
@@skip -- Go multi-system codegen not yet working

package main

import "fmt"

@@system LoginManager {
    interface:
        Login(user: string, pass: string): string

    machine:
        $LoggedOut {
            Login(user: string, pass: string): string {
                @@:("ok")
            }
        }
}

@@system App {
    interface:
        Authenticate(user: string, pass: string): string

    machine:
        $Unauthenticated {
            Authenticate(user: string, pass: string): string {
                @@:("ok")
            }
        }

    domain:
        auth : LoginManager = @@LoginManager()
}

func main() {
    fmt.Println("SKIP: auth_flow — Go multi-system codegen not yet working")
}
