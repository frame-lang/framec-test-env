@@target cpp_17
@@skip -- requires multi-system (ToolRunner + Agent)

#include <iostream>

@@system AiAgent {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}

int main() {
    std::cout << "SKIP: ai_agent — requires multi-system (ToolRunner + Agent)" << std::endl;
    return 0;
}
