@@target c
@@skip -- C does not support multi-system

#include <stdio.h>

@@system AuthFlow {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}

int main() {
    printf("SKIP: auth_flow — C does not support multi-system\n");
    return 0;
}
