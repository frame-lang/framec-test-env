@@target kotlin
@@skip -- too complex for single-file demo

@@system AiAgent {
    interface:
        dummy(): Int

    machine:
        $Start {
            dummy(): Int { @@:(0) }
        }
}

fun main() {
    println("PASS: ai_agent (skipped)")
}
