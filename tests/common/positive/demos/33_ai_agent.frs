@@target rust

// AI Agent -- multi-system + HSM + self-calls (too complex, stub)

@@system AiAgent {
    interface:
        dummy(): i32

    machine:
        $Start {
            dummy(): i32 { @@:(0) }
        }
}

fn main() {
    let mut a = @@AiAgent();
    let _ = a.dummy();
    println!("PASS: ai_agent");
}
