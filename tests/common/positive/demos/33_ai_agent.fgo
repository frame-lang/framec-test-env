@@target go
@@skip -- requires multi-system (ToolRunner + Agent)

package main

import "fmt"

@@system AiAgent {
    interface:
        Dummy(): int

    machine:
        $Start {
            Dummy(): int { @@:(0) }
        }
}

func main() {
    fmt.Println("SKIP: ai_agent — requires multi-system (ToolRunner + Agent)")
}
