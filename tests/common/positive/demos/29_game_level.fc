@@target c
@@skip -- C does not support multi-system

#include <stdio.h>

@@system GameLevel {
    interface:
        dummy(): int

    machine:
        $Start {
            dummy(): int { @@:(0) }
        }
}

int main() {
    printf("SKIP: game_level — C does not support multi-system\n");
    return 0;
}
