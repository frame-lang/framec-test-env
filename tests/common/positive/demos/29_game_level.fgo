@@target go
@@skip -- Go multi-system codegen not yet working

package main

import "fmt"

@@system EnemySpawner {
    interface:
        Spawn(): string

    machine:
        $Ready {
            Spawn(): string { @@:("enemy_spawned") }
        }
}

@@system GameLevel {
    interface:
        Start()

    machine:
        $Loading {
            Start() { -> $Playing }
        }
        $Playing {
        }

    domain:
        spawner : EnemySpawner = @@EnemySpawner()
}

func main() {
    fmt.Println("SKIP: game_level — Go multi-system codegen not yet working")
}
