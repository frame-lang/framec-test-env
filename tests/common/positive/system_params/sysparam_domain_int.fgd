@@[target("gdscript")]
extends SceneTree

@@system Counter(initial: int) {
    interface:
        increment()
        get_value(): int

    machine:
        $Idle {
            increment() {
                self.value = self.value + 1
            }
            get_value(): int {
                @@:(self.value)
                return
            }
        }

    domain:
        value = initial
}

func main():
    var c = @@Counter(10)
    c.increment()
    c.increment()
    var v = c.get_value()
    if v != 12:
        print("FAIL got " + str(v))
        return
    print("ok 1 - sysparam_domain_int")

func _init():
    main()
    quit()
