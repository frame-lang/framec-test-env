@@target php

<?php

@@system Robot(name: str, $(x: int)) {
    interface:
        describe(): str

    machine:
        $Start(x: int) {
            describe(): str {
                @@:($this->name . "@" . $x)
                return;
            }
        }

    domain:
        name = $name
}

$r = @@Robot("R2D2", $(7));
$d = $r->describe();
if ($d !== "R2D2@7") {
    echo "FAIL got $d\n";
    exit(1);
}
echo "ok 1 - sysparam_mixed\n";
