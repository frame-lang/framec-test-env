@@[target("csharp")]


@@system Robot($(x: int), name: string) {
    interface:
        describe(): string
    machine:
        $Start(x: int) {
            describe(): string {
                @@:(this.name + "@" + x) return;
            }
        }
    domain:
        name: string = name
}

class Program {
    static void Main(string[] args) {
        var s = @@Robot($(7), "R2D2");
        var v = s.describe();
        if (v != "R2D2@7") { Console.WriteLine($"FAIL got {v}"); Environment.Exit(1); }
        Console.WriteLine("ok 1 - sysparam_mixed");
    }
}
