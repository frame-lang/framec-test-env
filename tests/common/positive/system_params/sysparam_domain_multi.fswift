@@[target("swift")]

@@system User(name: String, age: Int, active: Bool) {
    interface:
        describe(): String

    machine:
        $Idle {
            describe(): String {
                @@:(self.name + "/" + String(self.age) + "/" + String(self.active))
                return;
            }
        }

    domain:
        name: String = name
        age: Int = age
        active: Bool = active
}

let u = @@User("alice", 30, true)
let s = u.describe()
if s != "alice/30/true" { fatalError("FAIL got \(s)") }
print("ok 1 - sysparam_domain_multi")
