@@[target("csharp")]

using System;

@@system Counter(initial: int) {
    interface:
        increment()
        get(): int
    machine:
        $Idle {
            increment() {
                this.value = this.value + 1;
            }
            get(): int {
                @@:(this.value) return;
            }
        }
    domain:
        value: int = initial
}

class Program {
    static void Main(string[] args) {
        var s = @@Counter(10);
        s.increment();
        s.increment();
        var v = s.get();
        if (v != 12) { Console.WriteLine($"FAIL got {v}"); Environment.Exit(1); }
        Console.WriteLine("ok 1 - sysparam_domain_int");
    }
}
