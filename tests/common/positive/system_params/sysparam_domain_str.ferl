@@target erlang

%% Test: single typed str domain param.

@@system Greeter(name: str) {
    interface:
        greet(): str

    machine:
        $Idle {
            greet(): str { @@:return = "hello, " ++ self.name }
        }

    domain:
        name = name
}
