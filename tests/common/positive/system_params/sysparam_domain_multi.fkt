@@[target("kotlin")]

@@system User(name: String, age: Int, active: Boolean) {
    interface:
        describe(): String
    machine:
        $Idle {
            describe(): String {
                @@:(this.name + "/" + this.age + "/" + this.active)
            return;
            }
        }
    domain:
        name: String = name
        age: Int = age
        active: Boolean = active
}

fun main() {
    val u = @@User("alice", 30, true)
    val v = u.describe()
    if (v != "alice/30/true") {
        println("FAIL got $v")
        kotlin.system.exitProcess(1)
    }
    println("ok 1 - sysparam_domain_multi")
}
