@@target erlang

%% Test: single typed bool domain param.

@@system Flag(initial: bool) {
    interface:
        toggle()
        get(): bool

    machine:
        $Active {
            toggle() {
                self.flag = not self.flag
            }
            get(): bool { @@:(self.flag) }
        }

    domain:
        flag = initial
}
