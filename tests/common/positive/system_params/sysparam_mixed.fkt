@@target kotlin

@@system Robot(name: str, $(x: int)) {
    interface:
        describe(): str
    machine:
        $Start(x: int) {
            describe(): str {
                @@:(this.name + "@" + x)
            return;
            }
        }
    domain:
        var name: String = name
}

fun main() {
    val r = @@Robot("R2D2", $(7))
    val v = r.describe()
    if (v != "R2D2@7") {
        println("FAIL got $v")
        kotlin.system.exitProcess(1)
    }
    println("ok 1 - sysparam_mixed")
}
