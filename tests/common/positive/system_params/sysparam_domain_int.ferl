@@target erlang

%% Test: single typed int domain param.

@@system Counter(initial: int) {
    interface:
        increment()
        get(): int

    machine:
        $Counting {
            increment() {
                self.value = self.value + 1
            }
            get(): int { @@:(self.value) }
        }

    domain:
        value = initial
}
