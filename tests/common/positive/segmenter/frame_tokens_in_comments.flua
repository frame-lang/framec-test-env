@@target lua
-- @@system FakeSystem { }
@@system FrameTokensInComments {
    interface:
        run_test()

    machine:
        $Start {
            -- -> $NotATransition
            -- => $^
            -- push$
            run_test() {
                -- These comments contain Frame tokens — should be ignored
                -- -> $FakeState
                -- => $^
                -- push$
                -- pop$
                -- $.var = 42
                -- @@:return = 99
                x = 1  -- -> $InlineComment
                y = 2  -- @@system Inline { }
                -- print
            }
        }
}
