@@[target("erlang")]
@@system NestedBraces {
    interface:
        run_test()

    machine:
        $Start {
            run_test() {
                % String containing braces — body closer must handle these
                S1 = "string with { braces }",
                S2 = "{\"json\": \"in string\"}",
                ok
            }
        }
}