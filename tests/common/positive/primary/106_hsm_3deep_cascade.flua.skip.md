# Intentional skip — Python-only RFC-0019 smoke

Python smoke fixture covering 3-deep HSM cascade per RFC-0019 (`#408` in framec memory). Same cascade behavior is exercised across all 17 backends by the matrix's broader HSM tests (tests 46, 47, 48, 49, 53, 61, 62 etc.).

See `docs/partial-coverage-audit.md`.
