@@target erlang
@@skip -- Erlang gen_statem persist is per-process; nested systems are nested processes — different arch from JSON-based persist

@@system L1 {
    interface:
        bump()
    machine:
        $S { bump() { self.n = self.n + 1 } }
    domain: n = 0
}
