@@[target("erlang")]
@@skip -- See tests/erlang/multi/persist_5deep/ — Erlang requires one module per file, so multi-system tests live in the multi/ directory layout

@@system L1 {
    interface:
        bump()
    machine:
        $S { bump() { self.n = self.n + 1 } }
    domain: n = 0
}
