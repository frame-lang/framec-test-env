@@target erlang

@@system DomainVars {
    interface:
        getName(): str
        setName(name: str)
        getCount(): int
        incCount()

    machine:
        $Active {
            getName(): str {
                return self.name
            }
            setName(name: str) {
                self.name = name
            }
            getCount(): int {
                return self.count
            }
            incCount() {
                self.count = self.count + 1
            }
        }

    domain:
        name = "default"
        count = 0
}
