@@target rust

// Tests @@:return in enter handlers (Rust version)
// Note: Rust uses native return pattern, chain semantics differ from Python/TS

@@system SystemReturnChainTest {
    interface:
        get_state_num(): i32

    machine:
        $Start {
            get_state_num(): i32 {
                return 1;
            }
        }

        $EnterSetter {
            get_state_num(): i32 {
                return 2;
            }
        }

        $BothSet {
            get_state_num(): i32 {
                return 3;
            }
        }
}

fn main() {
    println!("=== Test 15: System Return (Rust) ===");

    let mut s1 = @@SystemReturnChainTest();
    let state = s1.get_state_num();
    assert_eq!(state, 1, "Expected state 1, got {}", state);
    println!("1. get_state_num() = {}", state);

    println!("PASS: System return works correctly");
}
