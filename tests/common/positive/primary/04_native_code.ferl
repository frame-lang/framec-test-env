@@[target("erlang")]

@@system NativeCode {
    interface:
        compute(value: int): int

    machine:
        $Active {
            compute(value: int): int {
                @@:(value + 10)
                return
            }
        }
}
