@@target erlang

@@system NativeCode {
    interface:
        compute(value: int): int

    machine:
        $Active {
            compute(value: int): int {
                return value + 10
            }
        }
}
