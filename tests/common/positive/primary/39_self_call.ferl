@@target erlang
@@skip
%% Skip: Erlang @@:self not yet supported

@@system SelfCallTest {
    interface:
        calibrate(): str

    machine:
        $Active {
            calibrate(): str {
                @@:("skip")
            }
        }
}
