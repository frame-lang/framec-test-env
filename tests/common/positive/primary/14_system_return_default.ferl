@@target erlang
@@system ReturnDefault {
    interface:
        withDefault(): int
        withoutDefault(): int
    machine:
        $Active {
            withDefault(): int {
                @@:(42)
            }
            withoutDefault(): int {
                @@:(99)
                return
            }
        }
}
