@@target erlang
@@system HSMEnterChild {
    interface:
        getLog(): str
    machine:
        $Child => $Parent {
            $>() { self.log = self.log ++ ",child_enter" }
            getLog(): str { return self.log }
        }
        $Parent {
            getLog(): str { return self.log }
        }
    domain:
        log = "init"
}
