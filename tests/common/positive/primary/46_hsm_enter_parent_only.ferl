@@[target("erlang")]
@@system HSMEnterParent {
    interface:
        getLog(): str
    machine:
        $Child => $Parent {
            getLog(): str {
                @@:(self.log)
            }
        }
        $Parent {
            $>() { self.log = self.log ++ ",parent_enter" }
            getLog(): str {
                @@:(self.log)
            }
        }
    domain:
        log = "init"
}
