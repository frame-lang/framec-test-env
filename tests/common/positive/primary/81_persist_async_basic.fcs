@@target csharp
@@skip -- D18: csharp persist methods missing on @@async systems (framec defect)

using System;
using System.Threading.Tasks;

@@system PersistAsync {
    interface:
        configure(amount: int)
        get(): int

    machine:
        $Idle {
            configure(amount: int) { -> $Active }
            get(): int { @@:(0) }
        }
        $Active {
            get(): int { @@:(0) }
        }
}

class Program {
    public static void Main(string[] args) {
        Console.WriteLine("SKIP: 81_persist_async_basic -- csharp persist+async missing methods defect");
    }
}
