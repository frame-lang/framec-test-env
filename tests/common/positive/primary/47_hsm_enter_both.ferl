@@target erlang
@@system HSMEnterBoth {
    interface:
        getLog(): str
    machine:
        $Child => $Parent {
            $>() { self.log = self.log ++ ",child_enter" }
            getLog(): str { return self.log }
        }
        $Parent {
            $>() { self.log = self.log ++ ",parent_enter" }
            getLog(): str { return self.log }
        }
    domain:
        log = "init"
}
