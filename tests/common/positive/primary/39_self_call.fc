@@target c
@@skip
// Skip: C does not support @@:self (no reentrant dispatch)

@@system SelfCallTest {
    interface:
        calibrate(): int

    machine:
        $Active {
            calibrate(): int {
                @@:(0)
            }
        }
}

int main() {
    printf("SKIP: self_call -- C does not support @@:self (no reentrant dispatch)\n");
    return 0;
}
