@@target c
@@skip -- C dispatcher requires user-defined pack/unpack symbols per nested system type; out-of-scope for v1 5-deep test

#include <stdio.h>

@@system L1 {
    interface:
        bump()
    machine:
        $S { bump() { self->n = self->n + 1; } }
    domain: n: int = 0
}

int main() {
    printf("SKIP: 83_persist_5deep -- C nested-system persist requires per-type pack/unpack symbols\n");
    return 0;
}
