@@[target("rust")]

// NEGATIVE TEST: E113 - machine: before interface: violates canonical section order
// Expected: operations:, interface:, machine:, actions:, domain:

@@system S {
    machine:
        $A {
            e() { }
        }
    interface:
        e()
}
