@@[target("c")]

// NEGATIVE TEST: HSM circular parent chain
// Expected: E430 error from transpiler

@@system HsmCycleTest {
    machine:
        $A => $B {
            go() { }
        }
        $B => $A {
            go() { }
        }
}
