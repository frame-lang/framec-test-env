@@[target("typescript")]

// NEGATIVE TEST: Forward to parent in state without parent
// Expected: E403 error from transpiler

@@system ForwardNoParentTest {
    machine:
        $Start {
            event() {
                => $^
            }
        }
}
