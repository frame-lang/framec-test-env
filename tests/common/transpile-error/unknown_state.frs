@@target rust

// NEGATIVE TEST: Transition to undefined state
// Expected: E402 error from transpiler

@@system UnknownStateTest {
    machine:
        $Start {
            go() {
                -> $NonExistent
            }
        }
}
