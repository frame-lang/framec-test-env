@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — unescaped literal @ in a regex is misread as a Mode-C call-out (framec#100)
# expect-error: E732

@@fsm T(text: bytes) : bool = false { /@@target/ true }
