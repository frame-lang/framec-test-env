@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — a fallible match with a success transition needs a failure branch
# expect-error: E701

@@fsm T(text: bytes) : bool = false { /a/ -> $next  $next: true }
