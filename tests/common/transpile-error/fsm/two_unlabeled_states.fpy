@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — only the first state may be unlabeled
# expect-error: E704

@@fsm T(text: bytes) : bool = false { /a/ true : -> $e  /b/ true : -> $e  $e: false }
