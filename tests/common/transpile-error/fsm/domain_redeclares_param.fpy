@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — a domain field re-declaring a parameter must match its type
# expect-error: E707

@@fsm M(text: bytes) : int = 0 { /[0-9]+/ self.text  domain: text: int = 0 }
