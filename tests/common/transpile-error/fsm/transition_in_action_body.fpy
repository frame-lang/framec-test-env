@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — a -> transition inside an action body (RFC-0042 §3.7)
# expect-error: E712

@@fsm M(text: bytes) : bool = false { /a/ { -> $x } true : -> $x  $x: false }
