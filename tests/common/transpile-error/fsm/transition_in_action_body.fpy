@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — a -> transition inside an action body is rejected (parser E712, surfaced as E700)
# expect-error: E700

@@fsm M(text: bytes) : bool = false { /a/ { -> $x } true : -> $x  $x: false }
