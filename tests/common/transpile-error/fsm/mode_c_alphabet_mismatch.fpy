@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — Mode-C composition across mismatched alphabets
# expect-error: E731

@@fsm I(text: token) : bool = false { /IDENT/ true }
@@fsm T(text: bytes) : bool = false { /@I/ true }
