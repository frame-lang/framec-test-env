@@[target("python_3")]

# NEGATIVE @@fsm diagnostic — a stage capture label is used more than once in one state
# expect-error: E730

@@fsm M(text: bytes) : bool = false { $s: .n/[0-9]+/ .n/[a-z]+/ true }
