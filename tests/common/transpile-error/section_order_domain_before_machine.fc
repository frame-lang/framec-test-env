@@target c

// NEGATIVE TEST: E113 - domain: before machine: violates canonical section order
// Expected: operations:, interface:, machine:, actions:, domain:

@@system S {
    domain:
        int x = 0
    machine:
        $A {
            e() { }
        }
}
