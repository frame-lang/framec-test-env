@@[target("rust")]

// NEGATIVE TEST: E113 - actions: before machine: violates canonical section order
// Expected: operations:, interface:, machine:, actions:, domain:

@@system S {
    actions:
        do_thing() {
            x = 1
        }
    machine:
        $A {
            e() { }
        }
}
