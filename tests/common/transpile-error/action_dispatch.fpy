@@[target("python_3")]

# NEGATIVE TEST: Dispatch (=>) is forbidden in actions
# Expected: E401 error from transpiler

@@system ActionDispatchTest {
    machine:
        $Child => $Parent {
            go() { }
        }
        $Parent {
            go() { }
        }

    actions:
        helper() {
            => $^
        }
}

if __name__ == "__main__":
    pass
