@@target c
@@run-expect: TRANSITION:

#include <stdio.h>
#include <stdbool.h>

// Forward declarations (C requires declaration before use)
void native(void);
void x(void);

@@system S {
    machine:
        $A {
            e() {
                if (false) {
                    native();
                } else {
                    -> $B()
                }
            }
        }
        $B { }
}

// Stub functions for placeholder calls
void native(void) {}
void x(void) {}

// TAP test harness
int main(void) {
    printf("TAP version 14\n");
    printf("1..1\n");
    S* s = S_new();
    S_e(s);
    printf("ok 1 - else_transition_exec\n");
    S_destroy(s);
    return 0;
}
