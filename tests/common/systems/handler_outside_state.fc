// @skip Handler outside state is invalid Frame syntax - needs validator error
@@target c

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

@@system S {
    machine:
        e() {
            => $^
        }
}

int main() {
    S* s = S_new();
    S_e(s);
    S_destroy(s);
    printf("PASS\n");
    return 0;
}
