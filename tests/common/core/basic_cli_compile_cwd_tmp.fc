@@target c

@@system S {
    machine:
        $A {
            e() {
                -> $B()
            }
        }
        $B { e() { } }
}

int main() {
    printf("=== Test: basic_cli_compile_cwd_tmp ===\n");
    S* s = S_new();

    // Test that system instantiates and responds to event
    S_e(s);

    printf("PASS: basic_cli_compile_cwd_tmp\n");
    S_destroy(s);
    return 0;
}
