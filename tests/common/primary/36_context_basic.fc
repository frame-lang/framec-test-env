// @skip - Context syntax (@@.param, @@:return, @@:event) not yet implemented for C target
@@target c

// NOTE: This test is skipped for C target because the context syntax
// (@@.param, @@:return, @@:event) is Python-specific Frame V4 syntax
// that hasn't been implemented for C yet.

@@system ContextBasicTestSkipped {
    interface:
        is_alive(): bool

    machine:
        $Start {
            is_alive(): bool {
                ContextBasicTestSkipped_CTX(self)->_return = (void*)(intptr_t)1;
                return;
            }
        }
}

int main() {
    printf("=== Test 36: Context Basic (SKIPPED - @@ syntax not implemented for C) ===\n");
    printf("SKIP: Context syntax (@@.param, @@:return, @@:event) requires V4 C backend support\n");
    return 0;
}
