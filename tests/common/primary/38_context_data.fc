// @skip - Context syntax (@@:data) not yet implemented for C target
@@target c

// NOTE: This test is skipped for C target because the context data syntax
// (@@:data) is Python-specific Frame V4 syntax that hasn't been implemented
// for C yet.

@@system ContextDataTestSkipped {
    interface:
        is_alive(): bool

    machine:
        $Start {
            is_alive(): bool {
                ContextDataTestSkipped_CTX(self)->_return = (void*)(intptr_t)1;
                return;
            }
        }
}

int main() {
    printf("=== Test 38: Context Data (SKIPPED - @@:data syntax not implemented for C) ===\n");
    printf("SKIP: Context data syntax (@@:data) requires V4 C backend support\n");
    return 0;
}
