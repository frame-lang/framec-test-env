// @skip - Context syntax (@@.param, @@:return) not yet implemented for C target
@@target c

// NOTE: This test is skipped for C target because the context syntax
// (@@.param, @@:return) is Python-specific Frame V4 syntax
// that hasn't been implemented for C yet.

@@system ContextReentrantTestSkipped {
    interface:
        is_alive(): bool

    machine:
        $Start {
            is_alive(): bool {
                ContextReentrantTestSkipped_CTX(self)->_return = (void*)(intptr_t)1;
                return;
            }
        }
}

int main() {
    printf("=== Test 37: Context Reentrant (SKIPPED - @@ syntax not implemented for C) ===\n");
    printf("SKIP: Context syntax (@@.param, @@:return) requires V4 C backend support\n");
    return 0;
}
