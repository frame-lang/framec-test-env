@@target python

# Negative: inline #[target: python] annotations must error


@@system InlineTargetDirectivePython {
    interface:
        start()

    machine:
        $Init {
            start() {
                #[target: python]
                print("should not be allowed")
                return
            }
        }
}

# Stub functions for placeholder calls
def native(): pass
def x(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@InlineTargetDirectivePython()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - test_inline_target_annotation_python")
    except Exception as ex:
        print(f"not ok 1 - test_inline_target_annotation_python # {ex}")
