@@target python


@@system TripleQuotesPy {
    machine:
        $Init {
            start() {
                text = '''Line mentions -> $Next but should be ignored'''
                print(text)
                -> $Next
            }
        }
        $Next {}
}

# Stub functions for placeholder calls
def native(): pass
def x(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = TripleQuotesPy()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - test_mixed_body_triple_quotes")
    except Exception as ex:
        print(f"not ok 1 - test_mixed_body_triple_quotes # {ex}")
