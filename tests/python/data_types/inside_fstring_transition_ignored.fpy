@@target python

@@system S {
    machine:
        $A {
            e() {
                msg = f"transition token here: -> $B() ignored"
                msg2 = f"forward token here: => $^ ignored"
            }
        }
}

# Stub functions for placeholder calls
def native(): pass
def x(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@S()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - inside_fstring_transition_ignored")
    except Exception as ex:
        print(f"not ok 1 - inside_fstring_transition_ignored # {ex}")
