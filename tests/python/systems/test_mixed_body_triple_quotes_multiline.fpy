@@target python


@@system TripleQuotesMultilinePy {
    machine:
        $Init {
            start() {
                text = '''
This string mentions => $^ and pop$ and should not be segmented.
It also mentions -> $Other within the string.
'''
                print(text)
                -> $Next
            }
        }
        $Next {}
}

# Stub functions for placeholder calls
def native(): pass
def x(): pass

# TAP test harness
if __name__ == "__main__":
    print("TAP version 14")
    print("1..1")
    try:
        s = @@TripleQuotesMultilinePy()
        if hasattr(s, 'e'):
            s.e()
        print("ok 1 - test_mixed_body_triple_quotes_multiline")
    except Exception as ex:
        print(f"not ok 1 - test_mixed_body_triple_quotes_multiline # {ex}")
