@@[target("erlang")]

@@[persist]
@@[save(save_state)]
@@[load(load_state)]
@@system L5 {
    interface:
        bump()
        value(): int
    machine:
        $S { bump() { self.n = self.n + 1 } value(): int { @@:(self.n) } }
    domain: n: int = 0
}
