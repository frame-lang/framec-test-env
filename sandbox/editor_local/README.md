# Editor-local tests (migrated)

These test drivers and small FRM fixtures were previously kept in the VS Code repo
root for ad-hoc validation. They now live in the shared env to keep the editor
repo clean. Use the adapter_protocol harnesses for canonical checks; these remain
as supplemental examples.
